<script type="text/javascript">
  $(function() {
    /*$('#valid-codigo').hide();
    $('#valid-nome').hide();
    $('#codigo').blur(function() {
      if ($('#codigo').val() == '')
        $('#valid-codigo').show("slow");
      else
        $('#valid-codigo').hide("slow");
    });
    $('#nome').blur(function() {
      if ($('#nome').val() == '')
        $('#valid-nome').show("slow");
      else
        $('#valid-nome').hide("slow");
    });*/
  });
</script>
<%= form_for @cliente, :url => {:action => "create"} do |f| -%>
  <fieldset>
    <legend>Novo</legend>
    <table border="0" cellpadding="0" cellspacing="0"  id="id-form">
      <tr>
        <th valign="top"><%= f.label :codigo %></th>
        <td><%= f.text_field :codigo, :id => "codigo" %></td>
      </tr>
      <tr>
        <th valign="top"><%= f.label :nome %></th>
        <td><%= f.text_field :nome, :id => "nome" %></td>
      </tr>
      <tr>
        <th valign="top"><%= f.label :sexo %></th>
        <td><%= f.select :sexo, Cliente::SEXO %></td>
        <td>
			  </td>
      </tr>
      <tr>
        <td colspan="3"><%= f.submit "Salvar" %></td>
      </tr>
      <tr>
        <td colspan="3"><%= link_to "Cancelar", cliente_index_path %></td>
      </tr>
    </table>
  </fieldset>
<% end -%>

